---

- name: Restart nginx
  systemd:
    name: 'nginx.service'
    daemon_reload: yes
    state: restarted


- name: systemctl reload nginx
  systemd:
    name: 'nginx.service'
    daemon_reload: yes
    state: reloaded
